const UserController = require('./User.Controller');
const FollowController = require('./Follow.controllers');
const PublicationController = require('./Publications.controller');

module.exports = {
    UserController,
    FollowController,
    PublicationController,
}